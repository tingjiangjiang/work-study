1、查看首页列表的DOM结构
	1)网页上显示源代码查看【推荐】
	2)js代码中的ES6拼串的代码组织

2、把列表的DOM结构复制到首页的对应UL标签里面，构成静态页(注意把拼串的动态数据去掉换成假数据替代)

3、注释掉首页ajax获取动态列表的代码

4、修改编写db.js文件，新增分页查询功能(pagination)；该功能使用的是解构赋值的做法；在db.js文件中对该功能单独测试

5、修改首页index.js代码；如果首页的强力推荐和热映是一个json文件那直接查询数据库即可，返回的数据直接render渲染页面；如果首页的强力推荐和热映是两个json文件，那需要使用Promise.all来请求连个封装过的promise（分别是强力推荐的数据和热映的数据）

6、修改首页的数据模板（ejs），注意数据的输出务必和schema中描述的一致

7、使用排除法来解决模板的显示错误问题

8、重要！！！千万不要注释对应的代码，要删除或剪切

